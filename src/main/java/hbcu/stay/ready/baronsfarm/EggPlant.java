package hbcu.stay.ready.baronsfarm;

public class EggPlant extends Crop implements Edible {
    private Object EggPlant;

    @Override
    public Object yield() {
        super.yield();
        return (EggPlant);
    }
}
